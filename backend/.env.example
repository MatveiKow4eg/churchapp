# Backend env example
NODE_ENV=development
PORT=3000

# Database
# Для Prisma используйте DATABASE_URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app?schema=public

# Auth (на этом этапе не используется, но оставляем как основу)
JWT_SECRET=change_me

# Logging
LOG_LEVEL=info
