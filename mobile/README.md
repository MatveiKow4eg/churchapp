# Mobile (Flutter)

## Конфигурация
- Установите зависимости: `flutter pub get`

- Если используете env-файл (опционально):
  - `cp .env.example .env`

## Запуск
- `flutter run`

## Файлы
- `.env.example` — шаблон переменных окружения (опционально)
- `.env` — локальные значения (не коммитится)
